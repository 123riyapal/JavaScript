const accountId=23423;
let accountHolder="Riya";
var accountPassword="Mzn%12"
holderCity="Hyderabad";

// Const value never change if we do it throw an error;
// var we use rarely because it is not scopable;
// we use let 
// and we also declare variable without using any keyword but it is not a good practice

// if we want to print whole data in a rable form so we can use it console.table([]);
console.table([accountId,accountHolder,accountPassword,holderCity]);